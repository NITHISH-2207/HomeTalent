import React from 'react';
import Landing from '../shared/Landing.jsx';

export const Home = () => {
  // Renders landing page which serves as customer browse home
  return <Landing />;
};

export default Home;
